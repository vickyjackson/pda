### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame

# method name should be snake case
  def checkforAce(card)
    # condition should use == for comparison
    if card.value = 1
      return true
    else
      return false
    end
  end

# this should be def and should be properly indented
# arguments should be separated by a comma
  dif highest_card(card1 card2)
  if card1.value > card2.value
    # card should be card1
    # return card object instead of property to match method name
    return card.name
  else
    # add return statement before card2
    card2
  end
end
# this end should be moved to the bottom of the class
end

def self.cards_total(cards)
  # total should have a default value of 0
  total
  for card in cards
    total += card.value
    # move return statement outside of the loop
    return "You have a total of" + total
  end
end


```
